version: '3.5'
services:
    server:
        build:
            context: .
            target: server
        volumes:
            - ./server:/usr/src/app
        ports:
            - '5000:5000'
        env_file:
            - ./server/config.env
        tty: true
        command: yarn start
    client:
        build:
            context: .
            target: client
        volumes:
            - ./client:/usr/src/app
        ports:
            - '3000:3000'
        tty: true
        command: yarn start
